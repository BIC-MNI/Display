New features / modifications in Version 1.1 of MNI-Display:

    - faster painting
    - removed necessity of having Display.menu file, a default version
      is now compiled in.
    - slice cursor can be turned off
    - colour coding limits can now be inverted
